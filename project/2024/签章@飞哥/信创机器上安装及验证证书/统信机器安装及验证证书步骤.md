# 步骤如下
```shell
# 把 xjca.cer 文件使用openssl 转换成 xjca.crt 文件
openssl x509 -inform der -in xjca.cer -out xjca.crt
# 把xjca.crt文件放到 /oetc/ssl/certs 目录下(不一定是这个目录，到时候网上搜一下)
sudo cp -fr xcja.crt /etc/ssl/certs
# 更新证书信息
sudo update-ca-certificates
# 验证证书
sudo openssl verify /etc/ssl/certs/xjca.crt
````